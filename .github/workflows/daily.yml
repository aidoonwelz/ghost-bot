name: Ghost daily

on:
  schedule:
    - cron: "15 22 * * *"    # After the US market close; crypto still updates daily.
  workflow_dispatch:          # lets us run it manually / on demand

permissions:
  contents: write             # needed to commit the updated portfolio back

concurrency:
  group: ghost-daily
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run Ghost
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT: ${{ secrets.TELEGRAM_CHAT }}
        run: python ghost.py

      - name: Commit updated portfolio
        run: |
          git config user.name "ghost-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "portfolio update $(date -u +%FT%TZ)" || echo "no changes to commit"
          git pull --rebase
          git push
